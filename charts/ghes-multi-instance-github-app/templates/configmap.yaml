apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ghes-multi-instance-github-app.fullname" . }}-config
  labels:
    {{- include "ghes-multi-instance-github-app.labels" . | nindent 4 }}
data:
  instances.json: |
    {
      "instances": [
        {{- range $index, $instance := .Values.app.instances }}
        {{- if $index }},{{ end }}
        {
          "key": {{ $instance.key | quote }},
          "name": {{ $instance.name | quote }},
          "baseUrl": {{ $instance.baseUrl | quote }},
          "appIdEnv": {{ $instance.appIdEnv | quote }},
          "privateKeyEnv": {{ $instance.privateKeyEnv | quote }},
          "webhookSecretEnv": {{ $instance.webhookSecretEnv | quote }}
        }
        {{- end }}
      ]
    }

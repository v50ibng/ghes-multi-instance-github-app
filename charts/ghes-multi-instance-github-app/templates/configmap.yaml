apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ghes-multi-instance-github-app.fullname" . }}-config
  labels:
    {{- include "ghes-multi-instance-github-app.labels" . | nindent 4 }}
data:
  instances.json: |
{{ dict "instances" .Values.app.instances | toPrettyJson | nindent 4 }}
